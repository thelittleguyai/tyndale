# Reusable modules

Empty until staging + production land. When the second env duplicates a
significant pattern from dev, factor it into a module here. Candidate
modules: container_app, postgres_flexible, key_vault, monitor.
