# code_validator — placeholder

Full V1. Build via `docs/tyndale-spec/21_subagent_code_validator.md`. V1-Lite has Bill Detective do inline code lookups via tools (`ncci_check_pair`, `mue_check`, `qdrant_search_billing_codes`).
