# fhir_get_eobs

mode: full-only — not implemented in V1-Lite

This tool is part of Full V1. See `docs/tyndale-spec/22_tool_descriptions.md` for the full
specification. Build during Full V1 expansion when the FHIR / 1upHealth integration lands.

V1-Lite note: in V1-Lite this tool is not active. `upload_extract_eob` produces the same return
shape from uploaded documents. When FHIR goes live, this tool is added **alongside** the upload
tools — the upload path remains for users who prefer it.
