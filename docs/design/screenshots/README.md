# App screenshots — A8 before/after + RD-4 both-modes set

Captured 2026-08-17 from a LOCAL build (expo web export) against a local runtime with the
shared dev DB — stub auth, chat-first + record view on. 390×844 viewport, 2x scale, full-page.
`a8-after_*` = body 16px (commits a7b34f3 + 3668a9d) in light AND dark ("Clear day" /
"Midnight ledger"); `a8-before_light_*` = the pre-A8 build (3205ada) for the type-size diff.
Case screens use harness case 07ea096f (2 findings, 2-step gameplan). Data visible in shots is
synthetic (JORDAN Q. TESTPATIENT / seeded dev user) — no real PHI exists in this DB.
Reproduce: docs/build-kit/DEV_TEST_DAY.md §local-build.
