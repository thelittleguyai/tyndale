# Phil → Brock — canary ledgered · Human Review decisions absorbed · the file path, solved (2026-09-18)

**4 first, because it explains three months of "it's in the folder."** You were right to push. The absolute path we've been writing to is:

`/Users/phil/Tyndale V3/Tyndale Final/`

— and that folder is a **local unzip of `Tyndale Final.zip` from May 26**. It's a snapshot of the shared folder, not the shared folder. Nothing written there syncs anywhere; it was never going to reach you. On your side, everything you dropped reached me only when I downloaded it by hand — which is why your files "arrived" and mine didn't. Three drops, one cause, entirely on our end. Fix: I'm re-pointing our deliveries at the real synced folder today, then copying everything outstanding into it in one batch: the five A1–A7 files, doc 39 with your §7 decisions folded in, both mockups (v2), and this note. Reply with the exact path of the folder as *you* see it (Drive/Dropbox/iCloud — whichever) so we can confirm it's the same tree before I declare victory a fourth time. A1–A7 unblocks the moment those land.

**1 · Canary: ledgered, option (b).** The exact signature (analyst-notes field + code-family reasoning on siblings of a code actually on the bill) is allow-listed; everything else in that field still trips. The canary ledger is green — the last full sweep had zero marker hits — but I won't tell you the *sweep* is: it found two sweep-infrastructure problems, both fixed the same day. A deploy landing mid-sweep killed in-flight audits and left cases stranded in `audit_running` (now: a healer on a 15-minute schedule, and the sweep yields to a deploy instead of racing it), and the shared synthetic user ran into the 20-uploads/hour cap (now: one synthetic identity per run, torn down afterwards — and never enqueued for review, so a sweep can't fill your queue). Last full run: **22/23**. The one miss is `unbundled_panel` — the audit produced a cost-sharing arithmetic finding where the scenario expects a bundling one. That is an audit-content question, so it's yours, not infra's; case id on request. On the re-pick — agreed the set was badly chosen; 36000 is *venipuncture*, one of the most common codes in existence, which is embarrassing in hindsight. **Adopted 2026-09-18, verified against the billing_codes corpus and the CPT/HCPCS range shapes: 02417, 05821** (CPT format in the structurally unassigned gap between anesthesia and surgery) and **Z4411** (HCPCS format on the unassigned Z letter). Valid shape, never real, never in a family the engine reasons about — which also made the option-(b) allowance unreachable, so it's retired: with these three, *any* hit trips. Two of your files changed to match, because they still carried a `70553` worked example the harness no longer watches for: **`intelligence-layer/tools/descriptions/cost_estimate_tic.md`** (the Returns example's `code`) and **`intelligence-layer/prompts/chat_modes/per_case_mode.md`** (the "cite specific line items" example). Same one-token substitution their sibling files already carry; nothing else in either moved. The three older notes that told you to *keep* 70553/A9579/36000 (the 08-12 asks, the 08-17 sweep debrief, the 08-18 gather list) now carry a dated SUPERSEDED banner — ignore the old instruction wherever you meet it. The rule going forward is unchanged: if you swap an example code, tell us so the markers move with it (`intelligence-layer/prompts/README.md`).

**2 · Human Review: all seven decisions in, building Phase 1.** Doc 39 §7 now carries 2a–2g verbatim as the governing spec; mockup v2 has the three-way verdict and the fourth tab. Phase 1 (this cycle): queue with your 100%-default + sample dial + always-fire triggers, the three-pane workspace with the per-finding "why" expander, Approve / Disapprove / Can't verify with `unable_to_verify` excluded from the rate, disapproval requiring type + scope + **cause** + the three-prompt note, view-level audit logging, and the thread schema for your Phase 3 append-only corrections (marker + linked follow-up — copy is yours, nothing rendered until you write it). **Also Phase 1, landing now (09-21): the document viewer.** The first build showed you a document's metadata and not the document — a verdict without seeing the bill isn't a review. Each uploaded file is now a card that opens the stored image or PDF (at the cited page when a citation opened it) with a Document / OCR-text toggle, and every open is its own audit event. File cards, not page thumbnails: a thumbnail means pulling every patient document the moment a case opens, before you've asked to see any of them. The queue rows read as cases now too (provider · service date · which documents), with count pills for every state including Can't verify and Re-review. The **Data & provenance** tab ships in Phase 1 with the sections whose data exists today (documents, answers, retrieved chunks, priors); API pulls, live lookups, **Missing**, and **Retrieval misses** are Phase 2 — rendered as honest placeholders, not faked. You're right that Retrieval misses is the most valuable thing on the screen: it needs the retrieval layer to log what it *considered* and didn't apply, which is a small engine change — Phase 2's first item. Cause routing (content_gap → your queue, reasoning_error → engineering with the provenance snapshot, bad_input, stale_data_source) and the eval-stub-on-every-disapproval land in Phase 2 with it.

**3 · Script keys — noted, thank you.** All 16 plus capture, itemized, opener, A4, within both constraints. Registry ingests them the day they arrive.

**5 · Simplified intake FYI — acknowledged.** Building exactly as-is; engine, case file, cards, reveal, unlock, script all carry regardless of front door. Bring the decision when it's scoped.

**6 · One design-system note (yours to overrule).** The review pills exposed two text colours below AA: rose on rose-soft (3.51:1) and — worse, and nobody had listed it — amber-deep on amber-soft (3.21:1), which is also the legal draft banner and the landing's "Example" chip. I added a `rose.deep` step and re-derived `amber.deep` in the same hue so both clear AA on their soft tint, the way sage and citation already did; your five §A hexes are untouched. Values, contrast numbers and the guard that now enforces it are in `docs/design/palette_reconciliation.md` (addendum). If you want different shades, anything that keeps deep-on-soft ≥ 4.5:1 passes.
