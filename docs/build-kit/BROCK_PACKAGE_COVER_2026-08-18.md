# The Brock Package — cover memo (2026-08-18)

**Phil → Brock.** One send, everything owed, in dependency order. Engineering's board is clean: every open item in the product now routes through this package. The app is dev-complete — 955 runtime tests, a fully-accounted e2e sweep (every scenario green or carrying a dated note naming YOUR item it awaits), and two structural guarantees you specified are live and enforcing: copy renders verbatim from your file (drift fails CI by key name), and fabricated data is structurally dead (a code no document contains cannot reach a user — proven against a live fabrication the sweep caught).

## What's inside, and what each unblocks

| # | Item | Doc | Unblocks |
|---|---|---|---|
| 1 | ~~**§3.11 — the `unlock_more` copy**~~ **CLOSED 2026-08-27 note: authored in v1.1 (2026-08-18); the registry carries both keys verbatim and ZERO `[PLACEHOLDER-eng]` values — staging is no longer copy-blocked.** | `BROCK_ASKS` §3.11 | ~~The ONLY staging-boot blocker.~~ Done. |
| 2 | **The decision packet** (A1–A7 sign-offs with drafts ready · B1–B6 judgment calls · C sourcing · D confirms) | `brock_decision_packet_2026-08-12.md` + `COWORK_DRAFTS_ENG_REVIEW` | Eleven strings off engineering voice · X-rules CI · the 16-row design application · four conformance FAILs · crisis-routing resolution · `[B]` enforcement |
| 3 | **A6 — the error/all-clear taxonomy** (the X5 enum + all-clear vocabulary) | packet A6 / `37_x_rules_contracts_DRAFT` | Ends the stem-matching interim; X5 enforcement goes exact |
| 4 | **The priors table** (`missing_data_spectrum` in machine-readable form) | asks §3.5→promoted | **The literal activation switch for user-visible dollar ranges** — per-entry flag flip, zero code |
| 5 | **§3.10 — the never-substitute-example-codes prompt line** | asks §3.10 | The prompt-side half of the fabrication guard |
| 6 | **§3.9 + capture/email copy** (§3.6–3.8: capture keys, the two email bodies, the nudge split confirm) | asks §3.6–3.9 | Native + email surfaces off engineering voice |
| 7 | **The 50-state seed, pass 1** | your program item 3 | `enable_nsa_checks` → the balance-billing check activates (a ledger note prints on every sweep run until then) |
| 8 | **Rules/laws corpora content** | your program items | Detection quality: we just discovered dev ran with rules/laws/payer retrieval broken for six weeks — schemas now exist and degrade honestly, and we'll run a before/after sweep the day your content lands to measure the delta |

## Suggested order if you do one thing per sitting
~~§3.11 (ten minutes, unblocks staging)~~ *(closed 2026-08-27 — done in v1.1)* → the packet (most is approve/edit on ready drafts) → A6 + priors (both are "your numbers become the system's numbers") → the content program (seed, corpora) on its own cadence.

Every draft awaiting you was authored under one rule: voice was drafted, facts were never invented — where a number or citation was needed, you'll find a question, not an answer.
