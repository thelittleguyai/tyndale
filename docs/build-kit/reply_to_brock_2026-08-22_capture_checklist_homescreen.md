# Phil → Brock — re: capture buttons, audit loading screen, checklist upgrade, homescreen (2026-08-22)

All four accepted; three are already prompted for this build cycle. Point-by-point, plus one thing your screenshot caught that's better than a layout issue, and one honest position on Browserbase.

**1 & 4 — capture buttons.** Agreed on both: "Use this photo" + "Retake" become a proper side-by-side button row (no more hovering over the camera), same for "Done" + the next-page button, and "Take the next page" → "Take another picture." Shipping this cycle.

**2 — the weird loading screen.** Two things were happening at once and you caught both:
- The presentation rule you proposed is right and is being implemented: while the audit is running, the screen shows ONLY the "Working on your audit" card. Analysis bubbles and questions appear after the run completes or properly pauses — nothing overlaps a spinning card.
- The deeper catch: *"what's your relationship to Payments (since last statements)?"* — that string is a ledger row label from the bill's account-summary table that the extractor misread as the visit description, and the copy engine used it unvalidated. We're adding a plausibility gate: no extracted string enters user-facing copy unless it passes checks (bill-form furniture rejected), and on failure the copy degrades to the generic form ("this bill") instead of asking a question built on junk. Your test also surfaced that a summary-only statement page (yours was page 1 of 4) should route to the itemized-bill request path rather than a line-item audit — being verified in the same pass.

**3 — checklist upgrade: yes to all of it, and it's better than you know.** Per-item Add buttons, the coverage fields *(count note 2026-08-27: six items ship — the four numbers, coinsurance added by the audit sweep, plus visit-confirm; nine explainer keys total — the 08-26 reply's figures are the current ones)*, AI-suggested visit-confirmation chips, "What is this?" explainers, and seamless checklist↔chat with free text always available — all in this cycle's prompt. The part I want you to see: the four numbers you asked for (deductible, deductible met, OOP max, OOP met) are exactly the inputs your own Tranche 1 priors table names as the biggest swing drivers — "deductible met" is the load-bearing unknown in the materiality engine. So when a user fills these in, the audit's ranges tighten live per your tier contract. Your UX ask and your content program just met in the middle. The explainer copy ships as script keys you own — interim copy now, your rewrite whenever.

**Browserbase — hold, and here's the honest reasoning.** Tyndale driving the user's insurer portal means handling their portal credentials and screen-scraping PHI through a third-party browser vendor — that's a new BAA, a credential-custody policy, counsel review, and a security surface bigger than anything else on the launch list, for data we can mostly get two cheaper ways: (a) the user types four numbers into the new checklist (10 seconds each with the explainers), and (b) the 1upHealth coverage connection — already built and gated — pulls deductible/OOP positions from the payer automatically with none of the credential custody, since the user authenticates at the payer directly. Proposal: launch with checklist + explainers, fast-follow the coverage connection (it's already on the post-launch list), and revisit Browserbase only if real users demonstrably stall on both. If you feel strongly it's a launch differentiator, let's put it through the decision-packet process with counsel in the room — not sneak it in as a UI feature.

**Homescreen mockups — good direction, shipping the honest subset now.** Going in this cycle: the header with "+ Check a bill," recovered-to-date and open-cases stat cards, case cards with real status pills (including deadline pills where a real deadline exists), the quick check-in card with your outcome chips (wired to the outcomes machinery that already exists), and the persistent chat button. Held back, with reasons you'll recognize:
- "Deadlines watched, numbers re-checked" banner copy — that's a proactive-monitoring (B8) claim and B8 isn't built; the banner will state true things about case status until it is. Rewrite welcome copy within that constraint anytime — it's a script key.
- Estimate Costs / Find a Doctor / Plan a Visit tiles — those are §5 features not yet started; dead buttons are worse than absent ones. The grid grows as features land.
- The deductible/OOP progress bars — ship only when powered by real values (user-entered via the new checklist, SBC-derived, or the coverage connection), labeled with their source. Never from a prior.

**Needed from you (all copy, all script keys):** explainer copy for the seven checklist items, outcome-chip labels, welcome-banner copy within the honesty constraint, and the opener/chip copy still open from the last note. Plus the standing items: A1–A7 review, Tranche 2 against the confirmed schema, rubric.
