// Re-export the shared Tyndale design tokens so admin components import from a
// single local path (per the CO-6A file layout). The Tailwind palette in
// tailwind.config.ts already mirrors these.
export * from '@tyndale/shared';
